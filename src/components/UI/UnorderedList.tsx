import {
  UnorderedList as ChakraUnorderedList,
  ListProps as ChakraUnorderedListProps,
} from "@chakra-ui/react";

export function UnorderedList(props: ChakraUnorderedListProps) {
  return <ChakraUnorderedList {...props} />;
}
