import {
  PinInputField as ChakraPinInputField,
  PinInputFieldProps as ChakraPinInputFieldProps,
} from "@chakra-ui/react";

export function PinInputField(props: ChakraPinInputFieldProps) {
  return <ChakraPinInputField {...props} />;
}
